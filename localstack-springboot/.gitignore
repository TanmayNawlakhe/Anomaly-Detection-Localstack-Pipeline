# Module build outputs
.gradle/
build/
out/
target/

# Python cache inside module (worker tooling)
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Terraform local cache/state under module
infra/terraform/localstack/.terraform/
infra/terraform/localstack/*.tfstate
infra/terraform/localstack/*.tfstate.*

# Intentionally keep .env files tracked
